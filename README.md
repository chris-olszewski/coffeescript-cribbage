coffeescript-cribbage
=====================

Cribbage written in coffeescript

Commands used to build:
  - `coffee --join lib/compiled.js -wc src/`
  - `coffee --output lib/ -wc main.coffee`
  - `nodeunit test`
